name: Functional Tests

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test-save-load:
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test echo
        run: |
          echo "Hello from functional tests"
          echo "This is a test"
